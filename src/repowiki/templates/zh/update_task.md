---
id: {{TASK_ID}}
kind: page_update
phase: 2
title: {{TITLE}}（增量更新）
output: {{OUTPUT}}
hint_files:
{{HINT_FILES_YAML}}
---

# 任务：增量更新 Wiki 页面「{{TITLE}}」

把更新后的页面**整体重写**到：<b>{{OUTPUT_ABS}}</b>（相对仓库根：{{OUTPUT}}）。
**只写这一个文件；不得改动仓库源码或其他 wiki 页面。**

## 自上次生成以来变更的文件（本次更新的依据；请重读它们的当前内容）
{{CHANGED_FILES}}

## 本页要点（page_brief，仍然有效）
{{PAGE_BRIEF}}

## 当前页面全文（在它基础上更新，保留仍然正确的部分）
```markdown
{{OLD_PAGE}}
```

## 更新规则
1. 在 H1 之后、「目录」之前插入一个小节：

   ```markdown
   ## 更新摘要

   **变更内容**
   - <具体说明哪部分内容因哪个文件变更而更新，如：更新了 API 端点路径，从 /old 迁移到 /new>
   ```

2. 对照变更文件修正正文中过时的描述、行号区间与图表；未受影响的小节尽量保持原样。
3. 「目录」列表加入「更新摘要」条目（放在「简介」之前）。
4. 若确有重大改进，可在「结论」小节相关条目后追加 `**已更新**` 标记。
5. 其余结构、文风与引用规范与首次生成完全一致：

{{STYLE}}

## 硬性检查项
与首次生成相同（H1=「{{TITLE}}」、必备小节齐全、「更新摘要」存在、章节来源/图表来源链接真实且行号不越界、≥2 个 mermaid 图、页间零链接）。

撰写期间每隔几分钟执行 `repowiki touch <仓库路径> --task {{TASK_ID}}` 续期认领（长任务防被回收）。

完成后运行：`repowiki check <仓库路径> --task {{TASK_ID}}`
