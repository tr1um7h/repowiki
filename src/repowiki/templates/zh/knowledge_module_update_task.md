---
id: {{TASK_ID}}
kind: knowledge_module
phase: 2
title: 知识模块：{{TITLE}}（增量更新）
output: {{OUTPUT_DIR}}
---

# 任务：增量更新知识模块文档「{{TITLE}}」

模块目录：<b>{{OUTPUT_DIR_ABS}}</b>（相对仓库根：{{OUTPUT_DIR}}）。
**只写该目录下的三个文档；不得改动仓库源码或其他知识产出。**

## 模块信息
- 覆盖范围（scope）：{{SCOPE}}

## 自上次生成以来变更的文件（仅列出落在模块 scope 内的部分；请重读它们的当前内容）
{{CHANGED_FILES}}

## 需要重写的文件（每个都是简短文档，中文行文、技术名词保留英文）
1. `概述.md` —— 一两句话说明该模块是什么、在仓库中的角色。
2. `技术栈.md` —— 一段话列出语言/框架/构建工具/存储等关键技术选型。
3. `架构设计.md` —— 3~6 条 bullet，说明内部结构与对外契约（调用链、编排入口、跨进程契约等）。

## 更新规则
1. 对照变更文件修正过时描述；未受影响的文档保持原样即可（但仍须存在且非空）。
2. 篇幅克制：概述 1~2 句、技术栈 1~3 句、架构设计 3~6 条。
3. 引用仓库文件用相对路径，不用 emoji/表格。

## 硬性检查项
三个文件（`概述.md`/`技术栈.md`/`架构设计.md`）都存在且内容非空。

撰写期间每隔几分钟执行 `repowiki touch <仓库路径> --task {{TASK_ID}}` 续期认领（长任务防被回收）。

完成后运行：`repowiki check <仓库路径> --task {{TASK_ID}}`
